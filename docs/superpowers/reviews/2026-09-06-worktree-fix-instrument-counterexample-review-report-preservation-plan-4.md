## Plan Review

**Status:** Issues Found

**Previous findings:** 3 received — 0 still open: none. All three of round 3's blocking findings are fixed. Two new blocking findings, both collateral from the repairs themselves.

**Scope taken.** Round-4 scope per the template: I verdicted the three, read the seven changed regions at full bar, grepped every identifier they moved, and took step (d) as far as the plan admits. I did **not** reopen `file:line` citations in untouched tasks (Task 1's baseline, Task 2 Steps 1–6, Task 6, the `tests/hooks` conventions paragraph). Step (d): the only code the plan contains is the Task 2 Step 3 helper, and item 7 changed it, so I extracted it (93 lines — 91 at round 3, plus item 7's two comment lines), ran it, and re-ran the two named counterfactuals against the new text. Tasks 1, 3–8 specify no code, so there is nothing else to build; no suite this plan adds exists yet, so "run the new tests against the previous HEAD" has no subject this round.

**Verdicts on the three.**

1. **Task 3 Step 1 / Step 4 allocation — FIXED.** Verified against the plan's own Task 3 Step 5 rather than the dispatch's description. Step 5 asserts `scripts/check-review-reports.sh` exits 0 on the real ledger; measured, `docs/superpowers/review-yield.md:6-7` is a six-column header and separator (`| Date | Branch | Face | Round | Blocking findings | Still open from the previous round |`) with **35** data rows, so a `$0`-rooted gate misdirected at the real file returns 0 in every case. Step 1 (`:336`) now reads "cases 1, 2 and 3 expect non-zero and would fail loudly, and cases 4 and 5 expect 0 and would pass without their fixtures ever being opened", and Step 4 (`:364`) instruments cases 4 and 5. The allocation is now the right way round, and no case-1 residue survives. The "read it once by hand" alternative is gone, replaced by "Do not settle for reading it once by hand: a hand-read leaves the audit nothing". Collateral it created is A1 below.
2. **Task 5 Step 1 model sentence — FIXED.** `grep -niE 'transcript|irrecoverab|lost forever'` over the whole plan returns **exactly two** lines, `:71` and `:434`, both the T5.6 instrument definition itself; the model sentence at `:441` now ends "makes no claim about any **external file**", with the note "naming the kinds of external file it does not talk about would put those words in a document whose whole point is not to talk about them, and `T5.6` is red on exactly that". Checked against the spec: AC12 (`…-design.md:111`) requires only that the reference define `—` as "no report linked in this record", so the narrowed wording is still faithful. `git diff -G transcript` on `skills/requesting-code-review/references/review-yield.md` can no longer be tripped by text this plan supplies.
3. **Task 8 Step 4 "Three runners" — FIXED.** The count is now six, and it is right: `tests/claude-code/run-skill-tests.sh` is one file and `tests/explicit-skill-requests/run-*.sh` is five, exhaustively. `CLAUDE.md:130`'s "three that dispatch a live agent" is correctly distinguished as suites, and `tests/skill-behavior/` holds **0** `.sh` files, as the plan says. The specific defect no longer exists. **But the same sentence gained a new count, and that one is false — B1.**

**Issues (if any):**

- **[Task 8, Step 4]: the repair's closing sentence states a glob's yield that measurement contradicts by four files, and its second clause is false of those four.** The sentence (`:592`) reads "Six is the number of files a `tests/*/run-*.sh` glob actually sweeps in, and each one would hand this branch a non-deterministic red to interpret." Measured exhaustively, `ls -1 tests/*/run-*.sh` returns **ten**: the six named, plus `tests/antigravity/run-tests.sh`, `tests/compatibility-mode/run-tests.sh`, `tests/kimi/run-tests.sh` and `tests/opencode/run-tests.sh`. `find tests -name 'run-*' -type f` returns the same ten, so nothing is hiding outside the glob's depth. Those four are not live-agent suites and are not absent from CI — `.github/workflows/ci.yml` invokes them at `:105`, `:121`, `:124` and `:129` — so "each one would hand this branch a non-deterministic red" is false of four of the ten. This matters where the sentence sits: it is the justification for "**Take the list from `ci.yml`, never from a glob**", and an implementer who checks the claim runs the glob, sees ten, and finds the plan disagreeing with their terminal at exactly the step that is telling them not to trust the glob. The correct claim is one clause: the six named are the run-scripts CI does not invoke; the glob sweeps ten, four of which CI does run.

- **[Task 7, header line]: adding `T7.5` gave Task 7 a criterion in a class its `**Spec criterion:**` line does not declare.** The line at `:507` reads `**Spec criterion:** `AC17`, `AC20` `[structural]`.` — but `T7.5` (`:525`) is `IR7 [negative]`, and the matrix row agrees (`:81`, class `negative`). Every other task in this plan enumerates all of its criteria with their classes, including the mixed ones: Task 2 (`:139`) lists `[behavioral]`, `[structural]` and `IR4 [negative]`; Task 3 (`:314`) lists three classes; Task 5 (`:419`) lists `AC10…AC21 [structural]; IR7 [negative]`. Task 7 is now the only task whose header omits a criterion it carries, and it omits the only *negative* one — the class whose evidence rules differ. `scripts/task-brief` hands the task block downstream, so the implementer and the task reviewer read a header claiming this task is entirely structural while its fifth criterion is negative and must be settled by a read-only command. `check-cross-references` does not catch it: it reports IR7 cited (30 AC/IR cited, 41/41/41, 8 tasks, 24 `file:line`, exit 0) because IR7 is cited in Task 5. One line fixes it: `**Spec criterion:** `AC17`, `AC20` `[structural]`; `IR7 [negative]`.`

**Unverified External Calls (if any):**

- None. The plan's single external claim — POSIX `noclobber` `>` as an atomic `O_CREAT|O_EXCL` open, cited in the Step 3 comment against `pubs.opengroup.org` for Issue 8 / IEEE 1003.1-2024 — was confirmed at round 1 and re-fetched at round 3. Item 7 changed a `die` string eighteen lines below it and nothing in the citation block. Not re-fetched.

**Step (d), what was actually run.** Extracted helper, 93 lines, executes as a command (usage case: exit **2**, correct message). Item 7's branch, measured on both causes that reach it:

- Case 4's own fixture — a 300-character destination filename in an existing writable directory: unmutated exits **3**, stderr `save-review-report: cannot create <path>` with no clause about writability, and `ls -A` on the directory afterwards returns **0** files, so T2.4's "leaves no `-2`" still holds.
- The same fixture with `[ -e "$target" ]` replaced by `true`: exits **5**, message `every name up to …-99.md is taken`, **0** files written. The counterexample survives item 7 intact — case 4 still goes red on the mutation AC8 exists to prevent.
- Step 7's second, permission-based fixture (directory mode 555 holding an existing `r.md`, uid 1000): unmutated exits **3**, message `cannot create …/r-2.md`, and `r.md` is byte-identical afterwards. **The new message is true of both causes**, which is what item 7 was for; the old one was false of this one's twin.

`scripts/check-links.sh` exits **1**, on exactly the four unresolved links Task 1 Step 1 predicts, in `…-spec-1.md` at lines 13, 23 and 29 (twice) — so that step's baseline is still accurate today.

**Recommendations (advisory, do not block approval):**

- **[Task 3, Step 3 vs Step 4]** Step 4's assertion needs an observable the gate does not yet have, and Step 3 — the step that specifies the gate — does not mention one. Step 4 offers two routes: "have the gate name its ledger path in its output, **or** drive the case with a fixture whose distinguishing property the real ledger does not share". For case 4 the second route has no subject: case 4's fixture is a six-column ledger and so is the real one, both exit 0 and both are silent, so there is no property to drive on. Case 5 is the same on a different shape. Both cases collapse onto the first route, which means Step 3 must produce a gate that says which ledger it read. Say so in Step 3, where the gate's contract is written, rather than leaving it to be retrofitted at Step 4.
- **[Verification Matrix, T7.5]** The row's second conjunct — "no file under `docs/superpowers/reviews/` holds content this slice did not itself receive from a review dispatch" — is a provenance judgement, not a read-only command, and the `negative` class asks for a command over the declared scope. The first conjunct is executable and carries the row; consider stating plainly that the second is a recorded reading, so the auditor is not looking for a command that was never written.
- **[Verification Matrix, T5.6]** The `grep -rniE` half now enumerates "the four files the slice creates" as three files plus one directory (`skills/requesting-code-review/scripts/`). That is exhaustive and fine, but the directory is where a fifth created file would land unremarked; naming the file (`save-review-report.sh`) beside the other three would make the list read as the closed set it claims to be.

Remainder: 0.
