## Spec Review

**Status:** Issues Found

**Previous findings:** 4 received — 0 still open: none

**Scope taken.** Round-3 scope per "Which Round This Is": I verdicted the four round-2 blocking findings, read the eleven listed change regions at full bar (every citation in them opened, every command in them re-run), grepped the changed identifiers (`AC19`, `AC20`, `AC21`, `section_sources`, the `check-links.sh` line numbers) for damage outside the diff, and took step (d)'s saving on everything else — I did not reopen `## Problem`'s naming measurement, AC1–AC18, IR1–IR9, or the `## Codebase Findings` paragraphs the change list does not name.

**Round-2 verdicts (all four fixed).**

1. **AC19 one-pass coverage — fixed.** AC19 now names both sets and both citations hold: `scripts/check-links.sh:71` is `TARGETS += sorted(str(p) for p in pathlib.Path("docs").rglob("*.md"))`, and `section_sources()`'s body at `:175-182` is the second collection, ending on `and not p.name.startswith("RESULT-")]`. I also traced that the exclusion AC19 asks for is safe for its own second sentence: `SECTION_TARGETS` is consumed only as a *source* corpus at `:420-422`, while destinations resolve independently through `resolve_section_path`/`section_slugs` at `:427-434` — so a report excluded from `section_sources()` does remain a valid link and section destination, exactly as AC19 claims. The preserved round-1 report does carry three section references in `SECTION_REF` form (lines 13, 23, 29), so the hazard AC19 now covers is real and not hypothetical.
2. **`Report` cell form — fixed.** AC21 exists, fixes the cell as a markdown link relative to the ledger's directory, and cites `scripts/check-links.sh:188`, which is `LINK = re.compile(r"(?<!\!)\[[^\]]*\]\(([^)\s]+)(?:\s+\"[^\"]*\")?\)")` — link syntax and nothing else, as claimed. The "relative to the ledger's own directory" half checks out too: the four live failures resolve link paths against the containing file's directory. `CLAUDE.md:53` is section "Writing a reference" and `:57` reserves backticks for paths not meant to resolve, so Q9's source citation holds.
3. **Q5 recommendation source — fixed.** The `Source:` line is present and declares general practice as such, which the Coverage Map rules admit.
4. **"three links" — fixed.** Run today, the four unresolved links sit on three lines (13, 23, 29×2). But see the issue below: the two companion totals in that same sentence no longer reproduce.

**Issues (if any):**

- **`## Codebase Findings` — the repaired sentence still states two totals the named command contradicts today.** Line 211 reads "`check-links.sh` reports 816 links across 120 files and exits 1", in the present tense. Run in this tree at `35bab0b`: **815 local links across 121 files**, exit 1. Both numbers moved inside this very repair cycle — backticking the spec's own illustrative link took 816 → 815, and saving the round-2 review report took 120 → 121 — and the sentence was edited (item 4) without re-running the command. This is the same defect class round 2 blocked on in this same sentence, so it takes the same verdict; it changes no criterion, and the reproduced half ("four links, across three lines … exits 1") is what actually motivates AC19. Three clean repairs: re-measure to 815/121, date the figure the way the neighbouring paragraph dates its own ("Proved by difference on 2026-09-06"), or drop the two totals entirely — the file count is mutable **by this design's own construction**, since every report the slice saves increments it, which is the identical argument the spec already accepted at Q7 for the transcript count.

**Unverified External Claims (if any):**
- None. I re-fetched `https://pubs.opengroup.org/onlinepubs/9799919799/utilities/V3_chap02.html` — the vendor's own domain, headed "The Open Group Base Specifications Issue 8 / IEEE Std 1003.1-2024" — and § 2.7.2 carries both halves the row quotes, verbatim: "shall fail if the *noclobber* option is set … and the file named by the expansion of *word* exists and is either a regular file or a symbolic link that resolves to a regular file", and "The check for existence, file creation, and open operations shall be performed atomically as is done by the *open()* function … when the O_CREAT and O_EXCL flags are set". The `>|` form is documented on the same page as the second general format for output redirection, with the failure stated only for `'>'`. The `## Assumptions to Confirm` → "None" and the "or a symbolic link resolving to a regular file" wording carried into `## External Dependencies` are both correct. Q7's surviving existence claim also holds: transcript directories for this project exist with `.jsonl` files dated 2026-08-08 through 2026-09-06, covering the historical rows' dates.

**Recommendations (advisory, do not block approval):**
- `## Coverage the validation does not reach`, opening sentence: "every one of the ten resolved" is inexact — nine rows carry `Resolved` and "Integrations and external dependencies" carries `Clear`. The sentence's actual point (no category is marked out of reach) is true; "every one of the ten settled" would say it without colliding with a defined state name.
- Decision record Q8 still reads "Recommended: exclude … from the link-source targets **only**", which the Answer then corrects two lines down. Not a live contradiction — the Answer is unambiguous and AC19 is what downstream traces — but a reader who stops at the recommendation gets the superseded scope. A parenthetical on the Recommended line would close it.
- AC11, AC12, AC15, AC16 and now AC21 each bundle several independently checkable statements (AC21: the form, the relative base, and the stated reason). Same advisory rounds 1 and 2 raised; still not blocking, still cheaper to split before the plan traces them.
- The Coverage Map's ten categories are present, in the reference's order, with the glosses stripped as advised — that matches the reviewer template's conforming example exactly. No action.
