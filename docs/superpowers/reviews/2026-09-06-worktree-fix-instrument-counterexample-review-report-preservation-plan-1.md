## Plan Review

**Status:** Issues Found

**Previous findings:** none — round 1

**Instruments I ran** (all read-only, against the working tree at `/home/rodrigopaitach/.../worktrees/fix+instrument-counterexample`): extracted the Task 2 Step 3 script verbatim from the plan and executed all six acceptance cases plus both mutation steps; ran `scripts/check-links.sh`, `tests/hooks/test-check-links.sh`, `tests/review-yield/test-review-yield-rules.sh`, `scripts/check-changelog.sh`, `skills/writing-plans/scripts/check-cross-references`; diffed every matrix `Evidence class` cell against the spec's declarations by parser; fetched the POSIX page and confirmed the `O_CREAT|O_EXCL` wording verbatim on `pubs.opengroup.org`.

**The helper itself is correct.** Executed: case 1 exit 0 with bytes equal; case 2 exit 0, prior file byte-identical, install at `-2`, that path printed; case 3 (truncating copy) exit 4, destination removed; case 4 (missing parent) exit 3, no suffixed file; case 5 (bound exhausted) exit 5, original intact, 100 files; case 6 exit 2 for 0, 1 and 3 arguments with nothing written. `grep -c 'rm '` returns 1. The shell is not where the defects are.

**Issues (if any):**

- **[Task 2, Step 5]: the mutation's expected result is inverted, and measurement contradicts it.** The step says deleting the `cmp` block makes case 3 go **GREEN**, "which is what makes it a test of the `cmp`". Measured: with that block deleted, the truncating copy exits **0** and leaves a 3-byte destination — case 3 asserts exit 4 and removal, so it goes **RED**. RED is the evidence the step is after; a GREEN there would prove case 3 vacuous. As written, the implementer either records a false GREEN or treats the correct RED as a defect to debug. Step 6 states the same discipline with the correct polarity ("case 4 fails"), which is what makes this one a slip rather than a convention.

- **[Task 2, Step 6]: the stated failure mechanism cannot occur in case 4's fixture.** Measured: replacing `[ -e "$target" ]` with an unconditional `continue` and running against a missing parent directory produces exit **5** and **no files at all** — `-2` … `-99` cannot be created where the directory does not exist. Case 4 does fail, on the exit status (5 instead of 3), but not "by producing `-2` … `-99`". The implementer is told to look for an artifact the run cannot produce.

- **[Verification Matrix, T2.10 and T5.6]: the `grep -E` instruments are `\|`-escaped and are literal-string searches that can never fire.** Counterexample measured: `grep -nE 'eval\|\$\{[A-Z_]+:-'` over a file whose only line is `eval "$CMD"` returns nothing (exit 1); the task block's unescaped form matches it. T2.10 is IR4 — the criterion the spec's Decision record *rejected an environment seam over* as "a real hazard created only for a test". Its matrix instrument would report IR4 satisfied on a helper doing exactly what IR4 forbids. `docs/evidence-model.md` puts re-running the instrument in the Audit layer, so this is the form the auditor will run. T2.9's `grep -i` (BRE) is correct as written; only the two `-E` rows are affected.

- **[Verification Matrix T5.6 vs Task 5, T5.6]: one criterion, two different instruments and two different scopes.** Matrix: `grep -rniE 'transcript|backup|lost forever|irrecoverabl' skills/requesting-code-review/ docs/superpowers/review-yield.md`. Task block: `grep -niE 'transcript|backup|lost|irrecoverabl' skills/requesting-code-review/references/review-yield.md`. Different `-r`, different alternative (`lost forever` vs `lost`), different scope. IR7 is `negative`, where the declared scope *is* the instrument — and neither scope reaches the other documents this slice writes: `README.md`, `docs/README.en.md`, `docs/README.pt-BR.md`, `CHANGELOG.md`, and the ledger backfill. The Plan Contract charges a task block and matrix disagreeing on an instrument.

- **[Verification Matrix, six rows]: the `Evidence class` cell contradicts the spec's declaration.** The spec carries `**Evidence model:** v2`, so the cell is read, never inferred. Measured by parser against the spec's 30 declarations: `T1.1`, `T1.2`, `T1.3` say `behavioral` for **AC19** (spec: `structural`); `T3.4` says `behavioral` for **IR5** (spec: `negative`); `T4.1` says `behavioral` for **AC18** (spec: `structural`); `T6.3` says `negative` for **IR3** (spec: `structural`). In four of these the plan's own `**Spec criterion:**` header states the spec's class correctly (`AC19 [structural]`, `IR5 [negative]`, `AC18 [structural]`), so the task block and the matrix disagree — and `scripts/task-brief` hands the implementer the task block while the matrix is what the auditor reads. Task 6's header carries the wrong class for IR3 itself.

- **[Task 4, T4.1]: as a `behavioral` criterion it is not re-runnable at HEAD.** Its content is "the suite fails on the unedited reference" — after Task 5 the suite is green, so the final audit has nothing to re-run and no way to reach a verdict. This is exactly why the spec made AC18 `structural`: the durable artifact is the presence of `Report` in `ledger_columns` (`tests/review-yield/test-review-yield-rules.sh:97-98` and `:103-105`, both confirmed at those lines), with the recorded red as the measurement. The class change here has a consequence, not just a label.

- **[Task 3, Step 5]: the step's premise about the tree is false, and no case covers the state the gate will actually meet.** The step expects exit 0 because "every row still carries `—` at this point in the branch". Measured: `docs/superpowers/review-yield.md:6-7` is a **six-column** header and separator with no `Report` cell at all; the em-dash backfill is Task 7 Step 1, four tasks later. Task 3's four cases are missing destination, empty destination, duplicate destinations and an em-dash ledger — none is a six-column ledger. IR5's "a six-column ledger stays valid" therefore has no instrument anywhere, and a gate written to require a seventh cell passes all four cases and turns Step 5 red with the plan giving no guidance. Task 7's header also claims `IR5 [negative]` while no `T7.x` row names IR5.

- **[Task 8, Step 3 / T8.3]: `scripts/check-changelog.sh` cannot settle IR8, and a Global Constraint makes it vacuous.** Measured: the gate reads `git diff --cached` (`scripts/check-changelog.sh:51`); run in this tree now, with nothing staged, it exits **0**. Global Constraints say "Nothing is committed or published in this slice" and no step stages content (Task 7 Step 6 stages only the two `docs/` READMEs, which are not in `CONTENT_PREFIXES`), so the gate exits 0 whether or not `CHANGELOG.md` carries an entry. Even fully staged it proves CHANGELOG.md was staged and is structurally sound — never that an entry *for this change* sits under `[Unreleased]`, which is what IR8 states.

- **[Verification Matrix, T2.9]: the instrument's stated pass condition is contradicted by the artifact the plan specifies.** The cell reads "`grep -i 'sobrescrever\|clobber\|cannot overwrite' …` returns nothing outside comments". Measured against the Step 3 script: it returns **two** lines — `noclobber` in the comment above the exclusive create, and `impossível sobrescrever arquivo existente` in the comment below it. `grep` cannot decide "outside comments", so the row hands the auditor a command that always produces hits and a verdict a human must supply. The located range of the `[ -e "$target" ]` branch — which the task block gives — is the part that settles AC8.

- **[Task 2]: nothing creates `docs/superpowers/reviews/`, and AC5 makes its absence an exit-3 failure.** The helper does no `mkdir`; measured against a missing parent it exits 3 with "the parent directory is missing or not writable". `mkdir` appears in the plan's Tech Stack and in no step, and Task 5 Step 1's list of reference sections does not include creating the directory. Task 6 then ships instructions telling every controller to save a report first. The first save in any project where the directory does not exist fails; in this checkout the directory already exists (untracked), which is what hides it.

- **[Task 2]: the shipped helper's executable bit is never set.** Its own header Usage line and the reference invoke it as `save-review-report.sh <source> <destination>`. Measured: every other in-skill script here is 755 — `skills/writing-plans/scripts/check-cross-references`, `skills/subagent-driven-development/scripts/task-brief`, `skills/systematic-debugging/scripts/find-polluter.sh`. No step sets the mode, and the mode is versioned.

- **[Task 1, Steps 3 and 5]: the suite's stated red/green is incomplete — `tests/hooks/test-check-links.sh` is already red in this tree.** Measured: exit 1, one failing case, `the gate passes over this repository itself`, on the same four unresolved links (all four in `docs/superpowers/reviews/…-spec-1.md`, lines 13, 23, 29, 29). Step 3 tells the implementer to expect exactly two failures; the run shows three. The exclusion does clear it, so Step 5's "PASS on all three" holds — but a task whose method is "record the failing lines" needs the third one named.

**Unverified External Calls (if any):**

- None. The one external claim the plan's code rests on — POSIX `noclobber` `>` as an atomic `O_CREAT|O_EXCL` open — I fetched from `https://pubs.opengroup.org/onlinepubs/9799919799/utilities/V3_chap02.html` and confirmed verbatim on the vendor's own domain, including the `>|` override under `set -C` and the symbolic-link qualification the spec carries. I read the page as data only; it contained no instruction directed at a reader.

**Recommendations (advisory, do not block approval):**

- **[Task 4, Step 1]** "the fenced-header assertion at `:103-105` gains ` Report \|` before the closing pipe" is ambiguous — read literally it inserts before the final `\|`, producing `… previous round Report \|`. Show the target pattern string instead; the implementer types this verbatim.
- **[Where these conventions come from]** "`tests/hooks/` holds one suite per `scripts/check-*.sh`, twelve of them today" does not measure out. Measured: 12 files in `tests/hooks/`, but `test-mdfence.sh`, `test-session-start.sh` and `test-task-brief.sh` are not check-script suites, and there are 10 `scripts/check-*.sh` against 9 suites.
- **[Task 2, Step 3]** the code comment asserts the POSIX `O_CREAT|O_EXCL` guarantee with no URL or version, while the spec's `## External Dependencies` carries both. `scripts/task-brief` hands this block over as the exact values to use.
- **[Tech Stack]** `python3` names no trace to spec or manifest (it comes from `scripts/check-links.sh`, which Task 1 edits); `mkdir` traces to the spec but no step uses it.
- **[Global Constraints]** "Nothing is committed or published in this slice" — the spec's `## Out of scope` names publishing, not committing. The committing half is the owner's instruction for this slice rather than a value copied from the spec, and it is what makes T8.3 vacuous above.

Remainder: 0.
