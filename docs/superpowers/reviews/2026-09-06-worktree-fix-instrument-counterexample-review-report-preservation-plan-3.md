## Plan Review

**Status:** Issues Found

**Previous findings:** 5 received — 1 still open: finding 3 (Task 3 Step 4's fixture check).

**Scope taken.** Round-3 scope per the template: I verdicted the five, read the nine changed regions at full bar, grepped every identifier they moved, and took step (d) as far as the plan admits. I did **not** reopen `file:line` citations in untouched tasks. Step (d): the only code in this plan is the Task 2 Step 3 helper, unchanged since round 2 and not re-extracted for its own sake — I extracted it (91 lines, `bash -n` clean) solely to settle the one claim the dispatch named, plus the two instruments item 6 touched. Tasks 1, 3–8 specify no code, so there is nothing to build in a scratch copy; I did not re-run the existing suites this round.

**Verdicts on the five.**

1. **T5.6 / IR7 instrument — FIXED.** The rescoping is faithful to the criterion, not just to the numbers: the spec's IR7 (`docs/superpowers/specs/2026-09-06-review-report-preservation-design.md:145`) reads "no **document it writes** makes any claim about any file outside this repository", so "the text this slice adds" is IR7's own scope and the whole-file form was the over-reach. The 16 pre-existing hits are out of scope by construction. Measured on the created side: `grep -niE 'transcript|irrecoverab|lost forever'` over the extracted helper returns nothing. The `-G` half is reachable — nothing is staged, so `git diff` is the working-tree edit. Collateral it created is B2 below.
2. **Task 3 Step 1 root resolution — FIXED.** The gate's root is now stated as `$0`-resolved, the suite is told to copy the script, and the `check-changelog.sh` idiom is named as the one not to follow with the correct reason. All five citations verified by opening them: `scripts/check-links.sh:40-42` (`REPO_ROOT=…$(dirname "$0")…` at :40, `cd` at :42), `scripts/check-evidence-line.sh:40`, `tests/hooks/test-check-links.sh:36-39` (the `cp "$SCRIPT_UNDER_TEST"` lands at :39), `tests/hooks/test-check-evidence-line.sh:71-74` (the `cp` at :73), `tests/hooks/test-check-changelog.sh:58-61` (the `cd "$repo" && "$SCRIPT_UNDER_TEST"` at :61), `scripts/check-changelog.sh:51`. The consequence sentence attached to it is wrong — B1.
3. **Task 3 Step 4's fixture check — STILL OPEN.** A check now exists, but it is pointed at the one class of case that cannot silently pass. See B1.
4. **Case 4's fixture — FIXED, and the claim item 4 rests on is measured true.** Extracted helper vs. the same helper with `if [ -e "$target" ]` → `if true`, both run against a 300-character destination filename in an existing directory: **unmutated exits 3, mutated exits 5**, and the mutated run walks to `…-99.md` without writing. `ls -A` on the directory afterwards: **0 files**, so T2.4's "leaves no `-2`" holds. The fixture also reaches the branch round 2 said no test reached — the unmutated stderr is `cannot create …aaa.md — the directory is not writable`. The old fixture is confirmed vacuous on the same mutation: **3 and 3**, byte-identical messages. Step 7's second, permission-based fixture reproduces exactly what the plan records by hand: unmutated **3**, mutated **5**, prior file byte-identical, run as uid 1000.
5. **T2.11 — FIXED.** Matrix and task block now name the same two commands with the same pass condition, and both are measured true against the Step 3 script: `grep -n eval` returns **nothing** (exit 1); `grep -n '\$('` returns **exactly two** lines, `45:dst_dir="$(dirname -- "$dst")"` and `78:        n=$((n + 1))`. `grep -c 'rm '` returns `1`, so T2.9 still holds.

**Issues (if any):**

- **[Task 3, Step 1 and Step 4]: the repair's consequence sentence inverts which cases go silently green, and Step 4 therefore instruments the only case that cannot.** Step 1 (`:333`) says that under the wrong idiom "three of the five would pass without their fixtures ever being looked at, and the six-column case would pass for the wrong reason"; Step 4 (`:361`) repeats "passes three cases silently" and prescribes the fix: "make case 1's failure message name the scratch path". Read against the plan's own Task 3 Step 5, which asserts `scripts/check-review-reports.sh` **exits 0** on the real six-column ledger, the allocation is backwards. A `$0`-rooted gate reading the real ledger returns 0 in every case, so cases **4 and 5** (which expect 0) pass without their fixtures, and cases **1, 2 and 3** (which expect non-zero) **fail loudly** — the implementer cannot miss them. Case 1 is one of the three that announce themselves. Instrumenting it proves nothing, and cases 4 and 5, the only two that can be green while blind, are left with no check at all. This is the defect round 2's finding 3 asked for a check against, and it survives the check that was added. The repair is to move the instrumentation to case 4 — assert that its ledger path, or the gate's reported ledger, is under the scratch tree — and to correct the two sentences to "two of the five pass silently; the other three fail loudly".

- **[Task 5, Step 1 vs Task 5, Step 3 / T5.6]: the model wording the plan supplies for AC12's `—` definition contains the exact token T5.6's new instrument forbids in added text.** Step 1 (`:435`) reads: "**`—` means exactly "no report linked in this record".** It is a statement about the ledger cell. It does not say a file was written, does not say one was not, and makes no claim about any **transcript**, backup or other external file." It sits in the same bold-lead form as the block three lines below it, which does say "Say why in the reference itself", so nothing marks where the text to write stops. Measured: `skills/requesting-code-review/references/review-yield.md` contains no `transcript` today, and it is a tracked file Task 5 modifies — so an implementer who carries that sentence into the reference makes `git diff -G transcript -- <edited paths>` return a line, and T5.6 goes red at Step 3, where the plan then tells them "**Do not substitute a whole-file grep**". Before item 1 this was invisible: the row was red on 16 pre-existing lines regardless. The narrowed instrument made the plan's own Step 1 wording the only thing that can fail the row. One word fixes it — say "any external file" and drop `transcript, backup or other`.

- **[Task 8, Step 4]: "Three runners under `tests/`" is a stated count contradicted by measurement, and the sentence contradicts itself.** Measured, exhaustively: `tests/claude-code/run-skill-tests.sh` is one file and `tests/explicit-skill-requests/run-*.sh` is **five** (`run-all.sh`, `run-extended-multiturn-test.sh`, `run-haiku-test.sh`, `run-multiturn-test.sh`, `run-test.sh`) — **six**, which is the number round 2 reported and the number the sentence's own "family" implies. `CLAUDE.md:130`'s "three" counts live-agent **suites**, and the third of those is `tests/skill-behavior/`, which contains no `.sh` at all, so no glob ever caught it. The operative instruction — take the list from `.github/workflows/ci.yml`, never from a glob — is correct and I verified its positive half: `ci.yml:132` runs `node tests/pi/test-pi-extension.mjs` and `ci.yml:40` carries the `tests/brainstorm-server` block, both of which a `tests/*/*.sh` glob misses. Only the count is wrong; "Six runners" is the whole fix.

**Unverified External Calls (if any):**

- None. The plan's single external claim — POSIX `noclobber` `>` as an atomic `O_CREAT|O_EXCL` open, cited in the Step 3 comment with the vendor URL and the Issue 8 / IEEE 1003.1-2024 identification — is unchanged since round 1, where it was confirmed on `pubs.opengroup.org`, and the Step 3 block is outside this round's change list. Not re-fetched.

**Recommendations (advisory, do not block approval):**

- **[Verification Matrix, T5.6]** `<edited paths>` is an angle-bracket placeholder inside a `negative` row's instrument, and "the four files the slice creates" is never enumerated anywhere in the plan (they are derivable — the two `Create:` entries and the two `Test:` entries of Tasks 2 and 3 — but nobody says so). `git diff -G transcript` with **no** pathspec is exhaustive over every tracked edit this slice makes, needs no list, and cannot drift as the file set changes.
- **[Verification Matrix]** IR7 has two halves — "Nothing in this slice reconstructs a historical report" **and** "no document it writes makes any claim about any file outside this repository" — and T5.6 covers only the second. Item 8 added T7.4 precisely to give AC20's second half a row; the same shape one criterion over is left uncovered. Task 7 Step 1's "nothing is reconstructed" is the natural home for it.
- **[Task 2, Step 3 helper]** measured on case 4's new fixture, the branch it now reaches prints `cannot create <path> — the directory is not writable` when the directory is writable and the name is too long. That message was unreached by any shipped test before item 4 and is now reached by one, so its wording is user-visible for a cause it misnames. The helper is otherwise untouched; "cannot create <path>" alone would be true of both causes.
- **[Task 3, Step 4]** "Assert on it, or read it once by hand" leaves the weaker branch open, and the hand-read leaves no durable artifact for the audit. With the target corrected per the first blocking finding, the assertion is the only form that survives a later refactor.

Remainder: 0.
