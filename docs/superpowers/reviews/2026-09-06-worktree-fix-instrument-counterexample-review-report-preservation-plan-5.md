## Plan Review

**Status:** Issues Found

**Previous findings:** 2 received — 0 still open: none. Both round-4 blocking findings are fixed. Two new blocking findings, both collateral from this round's own repairs — items 3 and 5.

**Scope taken.** Round-5 scope per the template: I verdicted the two, read the five changed regions at full bar, grepped the identifiers they moved, and took step (d) as the template intends it — by running the commands the changed paragraphs assert rather than reading the paragraphs. I did **not** reopen `file:line` citations in untouched tasks (Task 1, Task 2, Task 4, Task 6, Task 8 Steps 1–3 and 5–6), and I did not re-extract or re-run the Task 2 Step 3 helper, which is unchanged. No task adds code this round, so "run each test the plan adds against the previous HEAD" has no subject: the counterfactual set is empty and I say so rather than substituting a neighbouring run.

**Verdicts on the two.**

1. **Task 8 Step 4 closing paragraph — FIXED, and the count is right this time.** Measured, not read. `ls -1 tests/*/run-*.sh` returns **ten**; `find tests -name 'run-*' -type f` returns the same ten, so nothing hides outside the glob's depth. The four named — `tests/antigravity/`, `tests/compatibility-mode/`, `tests/kimi/`, `tests/opencode/` — are invoked at `.github/workflows/ci.yml:105`, `:121`, `:124`, `:129`, exactly the four lines cited, each one correct. All four are deterministic: `antigravity/run-tests.sh` (16 lines) loops `test-*.sh`, `kimi/run-tests.sh` (6 lines) calls one manifest test, `compatibility-mode/run-tests.sh` says so in its own header comment at line 4 ("Deterministic: nothing here dispatches an agent"), and `opencode/run-tests.sh` runs local files under its own directory. The remaining six are the live-agent ones, and none appears anywhere in `ci.yml`. `tests/skill-behavior/` holds **0** `.sh` files, as the paragraph says, and `CLAUDE.md:130` does count three live-agent suites. The two things the glob misses — `node tests/pi/test-pi-extension.mjs` (`ci.yml:132`) and the `tests/brainstorm-server` npm block (`ci.yml:39-43`) — are both real. The specific defect no longer exists and the new sentence survives its own instrument.
2. **Task 7 header line — FIXED.** `:509` now reads ``**Spec criterion:** `AC17`, `AC20` `[structural]`; `IR7 [negative]`.`` The class matches the matrix row at `:80` (`negative`) and the spec's own declaration (`…-design.md:145`, `**IR7 [negative]**`). Task 7 is no longer the one task whose header omits a criterion it carries. The `check-cross-references` counts the dispatch quotes correspond to exactly this text — I re-derived the `file:line` figure, which moved 24 → 25 because item 1 added one full `.github/workflows/ci.yml:105` citation while `:121`, `:124`, `:129` are bare.

**Issues (if any):**

- **[Task 3, Step 3]: the new paragraph's appeal to precedent is false of most of the gates it appeals to.** `:359` ends "Every other `scripts/check-*.sh` here prints a one-line summary on success; this one's carries the ledger path." Measured by running all ten on the current tree: **four** print a one-line summary on success — `check-escalation-shape.sh` ("6 carrier(s), 6 occurrence(s)…"), `check-evidence-line.sh` ("8 carrier(s), 9 occurrence(s)…"), `check-no-dispatch.sh` ("7 carrier(s) carry the clause…") and `check-skill-behavior-records.sh` ("records well formed", `scripts/check-skill-behavior-records.sh:131`). **Five print nothing at all** on success: `check-changelog.sh`, `check-docs-sync.sh`, `check-frozen-history.sh`, `check-skill-frontmatter.sh`, `check-skill-size.sh` — all four I could run to green returned rc=0 with empty output. And `check-links.sh` prints **three** lines, to **stderr** (`scripts/check-links.sh:449-451`). So the majority of "every other" prints nothing, and the one that prints most does not print one line. This is the same defect class the last two rounds blocked on, in the same position: a measured-sounding clause supplied as the justification for the rule the paragraph is introducing. It matters at exactly the point the paragraph is trying to bind: an implementer told "this is a contract, not a nicety" checks the precedent offered for it, finds five silent gates, and the sentence that was meant to make the contract feel routine instead makes it look invented. The contract itself is sound and Step 4 genuinely depends on it — the fix is to drop or correct the last clause, not the paragraph. Correct form, measured today: four of the ten print a one-line success summary, five print nothing, and `check-links.sh` prints three lines to stderr; this gate joins the first group and its line carries the ledger path.

- **[Verification Matrix, T7.5]: the row's new closing clause points the auditor at the wrong row for IR7's other half.** `:80` ends "This row is IR7's first half; the row above reaches only its second." The row above `T7.5` is **`T7.4`** (`:79`), which is `AC20` / `structural` / `scripts/check-docs-sync.sh` — and that row's own text says it exits 0 on an empty index and settles nothing. It reaches no half of IR7. The row that reaches IR7's second half is **`T5.6`** (`:71`), nine rows up, and Task 7's own criterion line says as much in words at `:526` ("the half of IR7 the reference's own negative check does not reach"). The halves themselves are assigned correctly against the spec — `…-design.md:145` is a conjunction, reconstruction (`T7.5`) and no-claim-about-outside-files (`T5.6`) — so what is wrong is only the pointer, but it is wrong in a `negative` row, where the declared scope *is* the instrument and the audit has nothing else to go on. An auditor following it concludes IR7's second half is discharged by a gate that reads `git diff --cached` on an empty index. Sharpening the case: the identical phrase one row up, at `:79`, resolves correctly to `T7.3` — the same three words are right at `:79` and wrong at `:80`. Fix: name the row (`T5.6`), which is what the matrix does everywhere else it cross-refers.

**Unverified External Calls (if any):**

- None. The plan's single external claim — POSIX `noclobber` `>` as an atomic `O_CREAT|O_EXCL` open, cited against `pubs.opengroup.org` for Issue 8 / IEEE 1003.1-2024 — sits in Task 2 Step 3, which this round did not touch. Confirmed at round 1, re-fetched at round 3. Not re-fetched.

**Step (d), what was actually run.** No code changed this round, so the instrument change was to execute the assertions instead of reading them. `ls -1 tests/*/run-*.sh` → ten; `find tests -name 'run-*' -type f` → the same ten; `.github/workflows/ci.yml` greps confirming the four invocations and the absence of the six; `find tests/skill-behavior -name '*.sh'` → 0; the contents of all four "deterministic" runners; and every one of the ten `scripts/check-*.sh` run individually, which is what produced the first finding. Working tree state: `git status --short --untracked-files=all` shows **only** the ten untracked slice documents — spec, plan, and eight preserved reports — and nothing modified, so every number above is against `55b9c73` plus this slice's untracked documents. `scripts/check-links.sh` exits **1** on the same four unresolved links in `…-spec-1.md` (lines 13, 23, 29 twice), which is the baseline Task 1 Step 1 predicts; its summary now reads 816 links across 128 files, both figures moved by the reports saved since round 3, which is the mutability the spec already accepted at Q7.

**Recommendations (advisory, do not block approval):**

- **[Verification Matrix, T5.6]** The enumeration item 5 added is correct and closed as an instrument — the four are exactly the `Create:` and `Test:` entries of Tasks 2 and 3, and nothing else in the plan creates a file — but the lead-in that introduces them is not: "the untracked files this slice creates" numbers **fourteen** today, not four (the four deliverables, plus the spec, the plan, and eight preserved reports). It is not idle: `grep -rniE 'transcript|irrecoverab|lost forever' docs/superpowers/reviews/` returns **seven** lines across five of those reports. Nothing is red, because the row runs on the four it names and the plan's position on reports is stated at `T7.5` (received from a dispatch, not written by the slice) — but a phrase that describes a set of fourteen and then calls four of them "the closed set" is the shape this branch keeps tripping over. "The four files this slice creates under `skills/`, `scripts/` and `tests/`" closes it in one edit.
- **[Task 8, Step 4]** The step's wording — "Then each `scripts/check-*.sh` its gates job invokes" — is precisely right and I checked it: the gates job runs **nine** of the ten (`ci.yml:172-222`), and `scripts/check-skill-behavior-records.sh` is the one it does not. That matters because that script is **red at `HEAD` right now**, independently of this slice: `RESULT-criteria-read-in-pairs.md` was measured 2026-08-24 and `skills/writing-plans/plan-document-reviewer-prompt.md` was edited 2026-09-06, so it exits 1 asking for a **Rule changed since** row. An implementer who reads "run every gate" rather than the step's actual list hits it and reads "Expected: all green" as this branch's failure. Naming it as a known pre-existing red — the way Task 1 Step 1 names its `check-links.sh` baseline — costs one clause.
- All three of round 4's recommendations were adopted: `T7.5`'s second conjunct is now labelled a recorded reading rather than an instrument, `T5.6` names four files instead of three plus a directory, and Task 3 Step 3 now carries the gate's reporting contract with Step 4's second route removed. Nothing from the previous round is unadopted and unanswered.

Remainder: 0.
