## Spec Review

**Status:** Issues Found

**Previous findings:** 10 received — 0 still open: none

**Scope note.** There is no fix diff — the spec is untracked and the repair was a full rewrite (12 AC + 10 IR → 20 AC + 9 IR, Coverage Map replaced, two sections added). I treated the entire document as diff-touched and read it at full bar: every one of the 19 `file:line` citations opened, every measurement in `## Problem` re-run, both `check-links.sh` passes exercised. Step (d)'s saving was not taken and I am not claiming it.

**Round-1 verdicts (all ten fixed).** (1) `## External Dependencies` now exists with three rows. (2) The Coverage Map now carries all ten canonical categories in the reference's order, states from the four (`Resolved`/`Clear`) each with a reason, the `Where it landed` column, and a decision record of eight questions — one residual defect below. (3) `## The report path` states the shape and defines every segment. (4) The naming measurement now states max multiplicity **1** on the raw Branch column and shows both commands; I ran them and reproduced every number exactly, including the eight distinct Branch cells and their counts (10/8/5/5/4/1/1/1) and the six normalized pairs. (5) The harness claim is withdrawn — the spec now says it makes no claim either way. (6) The test-edit-seen-failing requirement is AC18; the classify-by-destination requirement is AC8. (7) AC3, AC6 and AC8 are now `structural`, and AC3 carries the counterexample. (8) Exit statuses named: 2/3/4/5. (9) The three READMEs are AC20. (10) `check-links.sh` no longer reports the spec — confirmed by running it (815 links, 120 files, exit 1, all four failures inside the preserved report).

**Issues (if any):**

- **AC19 covers one of `check-links.sh`'s two passes, and the identical hazard in the other is uncovered.** AC19 excludes `docs/superpowers/reviews/` from "the files it reads links **out of**" — the link-resolution pass built at `scripts/check-links.sh:71`. The *section-reference* pass, `section_sources()` at `scripts/check-links.sh:175-182`, also collects `docs/**/*.md` and skips only `CHANGELOG.md`, the frozen history and `RESULT-*`. Preserved reports are therefore still charged by it. Measured: in a throwaway copy of this tree I appended one section reference naming a non-existent heading to the round-1 report and the gate reported `…reviews/…-spec-1.md:41: docs/evidence-model.md -> no heading matching section "A Heading That Does Not Exist"`. This is not hypothetical for a later report — the preserved round-1 report already carries three section references in the exact `SECTION_REF` form (`coverage-map.md`, sections "Categories" and "The four states", line 13; `docs/evidence-model.md`, section "The instrument must match the scope of the claim", line 23; `CLAUDE.md`, section "Writing a reference", line 29). Rename any of those headings and a verbatim record turns the gate red — the exact condition AC19 exists to resolve, and the precedent the spec cites (`RESULT-*`) lives in *that* pass, not in the one AC19 names. Either extend AC19 to both passes or state, as a decision, why the section pass is left charged.

- **The `Report` cell's format is never fixed, and three criteria depend on it.** AC12 adds the column and defines `—`; nothing says what a cell containing a report holds. AC15's gate has to resolve it, and AC19's stated consequence — "a ledger row linking a missing report still fails" — is true only if the cell is markdown-link syntax: `LINK` at `scripts/check-links.sh:188` is `(?<!\!)\[[^\]]*\]\(([^)\s]+)…\)` and matches nothing else, and this repository's own convention puts non-resolving paths in backticks, which no pass reads. A planner who writes the cell as a backticked path satisfies AC12 and AC15 and silently falsifies AC19. This is the same shape as round-1 finding 3 (the path shape), one level down.

- **`## Coverage Map` → decision record, Q5 — a recommendation with no declared source.** "Recommended (by me): an environment-supplied install command" carries no `Source:` line; the other seven questions all do. Blocking per the Coverage Map rule ("Undeclared and invented are indistinguishable"), and a one-line fix — the recommendation is general practice and can say so.

- **`## Codebase Findings` — "three links inside the report do not resolve" is four.** Run today: `scripts/check-links.sh` exits 1 with four unresolved links inside `docs/superpowers/reviews/2026-09-06-…-spec-1.md` (lines 13, 23 and two on line 29). Three *lines*, four links. The companion figures are consistent and check out — 816 links was the state before the spec's own illustrative link was backticked, 815 today, and 811 + the report's 4 = 815 — which is what makes the "three" the only number the command contradicts. It changes no criterion; correcting it is one word.

**Unverified External Claims (if any):**
- None. I fetched `https://pubs.opengroup.org/onlinepubs/9799919799/utilities/V3_chap02.html` and the quoted sentence is present verbatim on the vendor's own domain, identified there as The Open Group Base Specifications Issue 8 / IEEE Std 1003.1-2024, together with the `set -C` behaviour and the `>|` override. The bash 5.3.9 measurements were reproduced in the round-1 review and I did not re-run them.

**Recommendations (advisory, do not block approval):**
- `## Assumptions to Confirm` can be closed: the POSIX page was fetched during this round and the quotation holds. One wording nuance if you keep it — the page says the redirection fails when the file exists and is "a regular file or a symbolic link resolving to a regular file"; the spec says only "a regular file".
- Decision record Q7's "146 transcripts" is not reproducible here: 133 `.jsonl` files in the main project directory, 165 across all `superpowersplus` project directories. The set is mutable and the conclusion ("nothing is reconstructed") does not need the number — name the instrument or drop the count.
- `scripts/check-links.sh:175-182` contains the `RESULT-*` exclusion but not the sentence quoted beside it; "a gate red on one would force rewriting a record to stay green" is at `:163-164`, in the same docstring. Per the smallest-sufficient-range rule the cited range does not support the quoted half.
- `## Coverage the validation does not reach` opens with "These are the same two rows the Coverage Map marks" and then lists three items; the regularized map marks none as out of reach. Leftover from the rewrite.
- The Coverage Map rows carry the category glosses (`Domain and data model — entities, identity, lifecycle, volume`); `skills/brainstorming/references/coverage-map.md`, section "Categories" says the map in the spec carries the names, not the gloss.
- 1 further, not listed: AC11, AC12, AC15 and AC16 each bundle several independently checkable statements — the same advisory round 1 raised against their predecessors AC7 and AC10.
