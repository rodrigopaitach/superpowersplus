## Plan Review

**Status:** Issues Found

**Previous findings:** 12 received — 0 still open

**Scope taken, stated plainly.** Per the dispatch, I treated the whole document as diff-touched and read it at full bar; step (d)'s saving does not apply and I did not take it. Step (d) itself I took as far as the plan admits: the only code the plan contains is the Task 2 Step 3 helper, which I extracted verbatim, made executable, and ran through all seven acceptance cases plus both mutation steps plus two counterfactual mutations of my own. I did **not** build the whole plan in a scratch copy — Tasks 1, 3–8 specify no code, only prose steps, so there is nothing to apply. I ran instead every existing suite and gate the plan's expectations rest on.

**Verdicts on the twelve, all fixed.** (1) Task 2's `cmp` mutation is now Step 6 and reads RED — measured: with the block deleted the truncating copy exits 0 and installs 3 bytes, so case 3's `exit 4` assertion fails. (2) The state-discriminator mutation moved to its own fixture (Step 7) — measured: `[ -e ] → true` against an unwritable directory holding an existing file exits **5**; the unmutated helper on the same fixture exits **3**. (3) The `\|`-escaped `-E` instruments are gone — `grep -n eval` returns nothing (exit 1), `grep -n '\$('` returns exactly two lines (45, 78). (4) Task 5's T5.6 now defers to the matrix row. (5) All six class cells corrected; I spot-verified T1.1–T1.3/AC19, T3.4/IR5, T4.1/AC18, T6.3/IR2, T6.4/IR3 against the spec. (6) T4.1 is now `structural` with located ranges. (7) Measured `docs/superpowers/review-yield.md:6-7` is a six-column header and separator with 35 data rows — Step 5's premise now holds, Task 3 gained the six-column case, and Task 7 no longer claims IR5. (8) T8.3 is `structural` and names `check-changelog.sh` as *not* the instrument. (9) T2.9's instrument is now the located range plus `grep -c 'rm '`, measured `1`. (10) The helper `mkdir -p`s the destination directory; the exclusive create is untouched and `[ -e ]` still discriminates (control run above exits 3). (11) `chmod 755` is Step 4, and T2.12 tests it through behaviour. (12) Task 1 Step 1 records the pre-existing red — measured exactly: exit 1, one failing case `the gate passes over this repository itself`, four unresolved links at `…-spec-1.md:13,23,29,29`.

**Issues (if any):**

- **[Verification Matrix, T5.6 / Task 5, Step 3]: the IR7 instrument returns 16 hits on the untouched tree, before the slice writes anything.** The row says "Each returns nothing". Measured, `grep -rniE 'transcript'` over the declared scope: `README.md:46`, `docs/README.en.md:112` and **14 lines in `CHANGELOG.md`**. None is written by this slice — `README.md:46` is the condensed escalation-format transcript that has been there for releases, and the changelog hits are history. `irrecoverab` and `lost forever` do return nothing. This is the round-1 repair's own damage: widening the scope to `README.md`, the two `docs/` READMEs and `CHANGELOG.md` is what pulled the pre-existing text in. Separately, `and the [Unreleased] section of CHANGELOG.md` is not a scope `grep -rniE` can take — grep reads files, not sections — but the row fails on the two README hits regardless of how that clause is read. An auditor running this row gets a red that says nothing about IR7, and an implementer at Task 5 Step 3 gets a failure with nowhere to go.

- **[Task 3, Step 1 vs Step 3]: the scratch-tree idiom named cannot work for the gate the plan describes, and if the implementer follows it, all five behavioral cases read the real repository.** Step 1 says to follow `tests/hooks/test-check-changelog.sh`, "so the script takes its repository root the way its siblings do". Measured, the siblings do not agree: `scripts/check-changelog.sh` has no repo-root resolution at all — it reads `git diff --cached` in the cwd, which is why `tests/hooks/test-check-changelog.sh:61` can `cd "$repo" && "$SCRIPT_UNDER_TEST"`. Every whole-tree sibling resolves the root from `$0` (`scripts/check-links.sh:40-42`, `scripts/check-evidence-line.sh:40`), and their suites therefore **copy the script into the scratch tree**: `tests/hooks/test-check-links.sh:38-39` and `tests/hooks/test-check-evidence-line.sh:73-74`. Step 3 then points the implementer at `scripts/check-evidence-line.sh:1-40` — the `$0`-rooted one. A `$0`-rooted `check-review-reports.sh` run under Step 1's idiom reads `docs/superpowers/review-yield.md` in the real checkout for every case: T3.1, T3.2 and T3.3 pass without ever seeing their fixtures, and T3.4 passes for the wrong reason, since the real ledger is six-column today. Nothing in the plan states which root the new gate resolves.

- **[Task 2, Step 1 case 4 / T2.4]: case 4 no longer reaches the branch AC5 is about, and it is measurably vacuous on the half AC5 names.** With `mkdir -p` in front, a regular file at the parent path fails at the `mkdir` and exits 3 **before the suffix loop is ever entered**. Counterfactual measured: I mutated the helper so a non-collision create failure enters the suffix search instead of dying — the exact failure AC8 exists to prevent, and the one AC5's "leaves no `-2`, `-3` or any other suffixed file behind" charges. Against case 4's fixture the mutated helper prints the same message and exits **3**, byte-identical to the unmutated run. Case 4 cannot fail on that mutation. Consequently the `die "cannot create $target — the directory is not writable"` branch is reached by **no test in the plan** — only by my control run of Step 7, which the plan does not ask for. AC5's own wording is "an unwritable parent directory"; Step 1 forbids building the case on permissions (correctly, for root), so the plan needs a second case, or Step 7 needs its control run recorded, or the branch stays untested.

- **[Verification Matrix T2.11 vs Task 2, T2.11]: two instruments in the matrix, one in the task block, and the matrix's pass condition is contradicted by measurement.** Matrix: `grep -n eval …` **and** `grep -n '\$(' …`, "each returning only the two `$(dirname …)` and `$(…)` forms". Task block: "instrument: `grep -n eval` over the file returns nothing". Measured against the Step 3 script: `grep -n eval` returns **nothing** (exit 1) — not "the two forms" — and `grep -n '\$('` returns exactly two lines. So the matrix states a pass condition that is false for its first command, and the task block drops the second command entirely. `scripts/task-brief` hands the implementer the task block; the auditor reads the matrix. The Plan Contract charges a task block and a matrix disagreeing on an instrument.

- **[Task 8, Step 4]: "Run each `tests/*/run-*.sh` and `tests/*/test-*.sh`" sweeps in six live-agent runners CI deliberately excludes, and still misses two suites CI runs.** Measured against `.github/workflows/ci.yml`: the glob catches `tests/claude-code/run-skill-tests.sh` and the five `tests/explicit-skill-requests/run-*.sh`, none of which appears in any CI `run:` step — `tests/claude-code/run-skill-tests.sh:15` shells out to the `claude` CLI, and `tests/explicit-skill-requests/run-test.sh` dispatches a live agent per prompt. `CLAUDE.md` states why they are out: token cost and non-determinism. In the other direction the glob misses `tests/pi/test-pi-extension.mjs` (CI runs it under `node`) and every `tests/brainstorm-server/*.test.js`. Step 4 then says "Report any red with its output rather than working around it", so the implementer is told to treat a non-deterministic live-agent result as a branch signal.

**Unverified External Calls (if any):**

- None unreachable. The plan's one external claim — POSIX `noclobber` `>` as an atomic `O_CREAT|O_EXCL` open, cited in the Step 3 comment with the vendor URL and the Issue 8 / IEEE 1003.1-2024 identification — I confirmed verbatim on `pubs.opengroup.org` in round 1 and did not re-fetch; the citation text is unchanged in this revision. One note on form, not reachability: the code comment now carries the URL and the standard's version, which closes round 1's recommendation on that step.

**Recommendations (advisory, do not block approval):**

- **[Where these conventions come from]** the count is closer but still off by one: `tests/hooks/` holds 12 files, and **8** of them are suites for a `scripts/check-*.sh`. `test-check-cross-references.sh` is a suite for `skills/writing-plans/scripts/check-cross-references`, which is not under `scripts/`. The three named as "something else" are right.
- **[Tech Stack]** `scripts/check-links.sh:42` is `cd "$REPO_ROOT"`; the interpreter starts at `:44` (`python3 - <<'PY'`). `check-cross-references` passes this citation because it checks the file and the range, never the content.
- **[Task 7, T7.3]** AC20 has two halves — "describe the ledger's columns including the report" **and** "`scripts/check-docs-sync.sh` stays green". T7.2/T7.3 cover the first; the second is disclaimed rather than covered. The reasoning for the disclaimer is sound, but the auditor will read AC20 whole.
- **[Task 8, Step 1]** the one-line reason offered for the new gate — "whole-tree because a link breaks when its destination disappears" — is `check-links.sh`'s own reason, verbatim from `githooks/pre-commit:19-24`. It happens to be true of the report gate too, but an implementer copying it is copying another gate's justification.
- **[Task 2, Step 1]** "`mkdir -p` fails there with `ENOTDIR` for any user, root included" is reasoned, not measured in this review: every run above was as uid 1000. The claim is standard filesystem semantics, but the plan states it as a measurement.

Remainder: 0.
